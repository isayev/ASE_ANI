NOTICE: Python binaries built for python 3.5

1) Boost python version 1.63 pre-built binaries are included in lib dir. Keep in mind, it may not work depending on you system configuration.

2) Export the libraries with:

NC_ROOT="[path to dir]/NeuroChem-ANI1"
export LD_LIBRARY_PATH="$NC_ROOT/lib:$LD_LIBRARY_PATH"
export PYTHONPATH="$NC_ROOT/lib:$PYTHONPATH"

3) In the python scripti (pyNeuroChem-examples.py), set the variable wkdir to point to the DIR containing THIS file (you will get en error if you do not do this correctly)

4) Test with the supplied python script:

	pyNeuroChem-examples.py


